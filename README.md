# PNG-to-C-Byte-Array-Generator
Converts PNG images to byte arrays for C++. Bytes are grouped in lines of 10 hexadecimal values and saved to a .txt file. Ideal for embedding images in C++ code without relying on external files.
